FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npx expo export -p web --output-dir build-web

FROM nginx:alpine
COPY --from=builder /app/build-web /usr/share/nginx/html
COPY game/nginx.conf /etc/nginx/nginx.conf
EXPOSE 80